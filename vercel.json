{
  "build": {
    "command": "cd frontend && npm install && npm run build",
    "outputDirectory": "dist"
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
